#
# Ansible hosts file.
# This is standard Ansible inventory in INI format.
# Lives at /etc/ansible/hosts
# This file isn't strictly required if you have other inventory files.
#
[operations_host_ddev]
server.company.com ansible_connection=local
